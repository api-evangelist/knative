naftiko: 1.0.0-alpha2
info:
  label: Knative Eventing API — EventTypes
  description: 'Knative Eventing API — EventTypes. 2 operations. Lead operation: Knative List EventTypes. Self-contained Naftiko
    capability covering one Knative business surface.'
  tags:
  - Knative
  - EventTypes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KNATIVE_API_KEY: KNATIVE_API_KEY
capability:
  consumes:
  - type: http
    namespace: eventing-eventtypes
    baseUri: https://kubernetes.default.svc
    description: Knative Eventing API — EventTypes business capability. Self-contained, no shared references.
    resources:
    - name: apis-eventing.knative.dev-v1beta2-namespaces-namespace-eventtypes
      path: /apis/eventing.knative.dev/v1beta2/namespaces/{namespace}/eventtypes
      operations:
      - name: listeventtypes
        method: GET
        description: Knative List EventTypes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createeventtype
        method: POST
        description: Knative Create an EventType
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.KNATIVE_API_KEY}}'
  exposes:
  - type: rest
    namespace: eventing-eventtypes-rest
    port: 8080
    description: REST adapter for Knative Eventing API — EventTypes. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/apis/eventing-knative-dev/v1beta2/namespaces/{namespace}/eventtypes
      name: apis-eventing-knative-dev-v1beta2-namespaces-namespace-eventtypes
      description: REST surface for apis-eventing.knative.dev-v1beta2-namespaces-namespace-eventtypes.
      operations:
      - method: GET
        name: listeventtypes
        description: Knative List EventTypes
        call: eventing-eventtypes.listeventtypes
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createeventtype
        description: Knative Create an EventType
        call: eventing-eventtypes.createeventtype
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: eventing-eventtypes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Knative Eventing API — EventTypes. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: knative-list-eventtypes
      description: Knative List EventTypes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: eventing-eventtypes.listeventtypes
      outputParameters:
      - type: object
        mapping: $.
    - name: knative-create-eventtype
      description: Knative Create an EventType
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: eventing-eventtypes.createeventtype
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
