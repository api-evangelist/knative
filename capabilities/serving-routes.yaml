naftiko: 1.0.0-alpha2
info:
  label: Knative Serving API — Routes
  description: 'Knative Serving API — Routes. 4 operations. Lead operation: List Knative Routes. Self-contained Naftiko capability
    covering one Knative business surface.'
  tags:
  - Knative
  - Routes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KNATIVE_API_KEY: KNATIVE_API_KEY
capability:
  consumes:
  - type: http
    namespace: serving-routes
    baseUri: https://kubernetes.default.svc
    description: Knative Serving API — Routes business capability. Self-contained, no shared references.
    resources:
    - name: apis-serving.knative.dev-v1-namespaces-namespace-routes
      path: /apis/serving.knative.dev/v1/namespaces/{namespace}/routes
      operations:
      - name: listroutes
        method: GET
        description: List Knative Routes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createroute
        method: POST
        description: Create a Knative Route
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: apis-serving.knative.dev-v1-namespaces-namespace-routes-name
      path: /apis/serving.knative.dev/v1/namespaces/{namespace}/routes/{name}
      operations:
      - name: getroute
        method: GET
        description: Get a Knative Route
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: deleteroute
        method: DELETE
        description: Delete a Knative Route
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.KNATIVE_API_KEY}}'
  exposes:
  - type: rest
    namespace: serving-routes-rest
    port: 8080
    description: REST adapter for Knative Serving API — Routes. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/apis/serving-knative-dev/v1/namespaces/{namespace}/routes
      name: apis-serving-knative-dev-v1-namespaces-namespace-routes
      description: REST surface for apis-serving.knative.dev-v1-namespaces-namespace-routes.
      operations:
      - method: GET
        name: listroutes
        description: List Knative Routes
        call: serving-routes.listroutes
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createroute
        description: Create a Knative Route
        call: serving-routes.createroute
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/apis/serving-knative-dev/v1/namespaces/{namespace}/routes/{name}
      name: apis-serving-knative-dev-v1-namespaces-namespace-routes-name
      description: REST surface for apis-serving.knative.dev-v1-namespaces-namespace-routes-name.
      operations:
      - method: GET
        name: getroute
        description: Get a Knative Route
        call: serving-routes.getroute
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteroute
        description: Delete a Knative Route
        call: serving-routes.deleteroute
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: serving-routes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Knative Serving API — Routes. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-knative-routes
      description: List Knative Routes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: serving-routes.listroutes
      outputParameters:
      - type: object
        mapping: $.
    - name: create-knative-route
      description: Create a Knative Route
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: serving-routes.createroute
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-knative-route
      description: Get a Knative Route
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: serving-routes.getroute
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-knative-route
      description: Delete a Knative Route
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: serving-routes.deleteroute
      outputParameters:
      - type: object
        mapping: $.
